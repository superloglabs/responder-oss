import { AgentCreatePage } from "./agent-create";

/** Agent settings share the same editor and connection recovery as creation. */
export function AgentDetailPage() {
  return <AgentCreatePage />;
}
