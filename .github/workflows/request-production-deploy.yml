name: Request hosted deployment

on:
  push:
    branches:
      - main

permissions: {}

jobs:
  request:
    name: Request deployment
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      DEPLOY_TOKEN: ${{ secrets.HOSTED_DEPLOY_TOKEN }}
      SOURCE_SHA: ${{ github.sha }}
    steps:
      - name: Check deployment token
        id: configuration
        shell: bash
        run: |
          if [[ -z "${DEPLOY_TOKEN}" ]]; then
            echo "Hosted deployment is not configured yet."
            echo "enabled=false" >> "${GITHUB_OUTPUT}"
          else
            echo "enabled=true" >> "${GITHUB_OUTPUT}"
          fi

      - name: Dispatch hosted deployment
        if: steps.configuration.outputs.enabled == 'true'
        shell: bash
        run: |
          payload="$(jq -cn \
            --arg ref main \
            --arg source_sha "${SOURCE_SHA}" \
            '{ref:$ref,inputs:{source_sha:$source_sha}}')"
          curl --fail-with-body --silent --show-error \
            --request POST \
            --header "Accept: application/vnd.github+json" \
            --header "Authorization: Bearer ${DEPLOY_TOKEN}" \
            --header "X-GitHub-Api-Version: 2022-11-28" \
            --data "${payload}" \
            https://api.github.com/repos/superloglabs/responder/actions/workflows/deploy-production.yml/dispatches
